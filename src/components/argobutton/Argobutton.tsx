import { ButtonProps } from "@mui/material";
import { Button as MuiButton } from "@mui/material";
const Argobutton = (props: ButtonProps) => {
  return <MuiButton {...props} />;
};

export default Argobutton;
